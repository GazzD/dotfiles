[data]
  name = "GazzD"
  email = {{ promptStringOnce . "email" "Git email address" | quote }}

{{- if eq .chezmoi.os "linux" }}
  # Monitor layout identifier for this machine.
  # Used in dot_config/hypr/monitors.conf.tmpl
  # Examples: "single", "dual-horizontal", "laptop"
  monitor_layout = {{ promptStringOnce . "monitor_layout" "Monitor layout (single/dual-horizontal/laptop)" "single" | quote }}
{{- end }}
